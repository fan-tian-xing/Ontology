老师好，今天汇报的主题是**汽轮机安调资料整理与查询应用**，主要面向核电汽轮机机组的安装调试场景，构建可追溯的知识图谱与智能问答系统。

---

## 一、任务背景与整体架构

### 项目目标

我们的最终目标，是面向核电汽轮机本机和辅机的安装调试全场景，构建一套智能体系统，辅助业务人员快速解答安调过程中遇到的技术问题，比如尺寸校验、工序要求、检修项目等。

现阶段的前期目标，是以《汽轮机本体安装及维护说明书》这一份核心领域资料为知识库，构建结构化的知识图谱，让智能体能够基于官方说明书给出规范、可追溯的处理建议。

### 整体技术链路

整个项目从原始资料到最终问答，分为四个核心环节，形成一条完整的闭环链路：

**1. 资料来源：唯一正式依据** 我们所有知识的唯一正式来源，就是原始扫描版的《汽轮机本体安装及维护说明书》。我们对全部 94 个物理页进行了逐页人工复核，配合 OCR 做辅助识别，确认每一页的文字、图表、页码、章节编号都准确对应，全程保留物理页号和原始出处，确保所有知识都能回溯到原文。

**2. 内容整理：把原文拆解为知识单元** 这一步的核心是 “结构化”—— 我们不再把说明书当成连续文本，而是按照工程语义，把内容拆分成对象、工序、步骤、参数等知识单元。每一条事实都会绑定对应的 SourceSpan 和 Evidence，也就是记录它出自哪一页、哪一章节、原文片段是什么，保证可回查、可溯源。

**3. 质量控制与入库：校验通过再发布** 知识单元不能直接入库，必须先过质量关。我们先冻结了本体 Ontology 和 Schema，也就是提前定义好允许的节点类型、关系类型、字段格式；然后通过 Validator 校验器，自动检查每一条记录的结构、数值、单位、来源是否完整合规；最后通过 Loader 以事务方式写入 Neo4j 图数据库，写入失败会自动回滚，保证图谱数据的一致性。

**4. 查询应用：从问题定位到结果核对** 用户提问后，系统先做问题解析，识别出问题涉及的设备、工序、参数或检查项；再通过 Neo4j 检索定位相关的知识对象和原始出处；最后经过结果校验，输出 “结论 + 物理页号 + 对应章节” 的完整回答。如果资料里没有明确说明，系统会标注出来，转人工复核，绝不臆造答案。

---

## 二、项目成果展示

接下来第二部分，项目成果展示，我会从知识图谱构建和智能体工作流两方面展开。

### 2.1 知识抽取与图谱构建

先讲图谱是怎么一步步建起来的。整个过程遵循 “先整理校验，再写入图谱” 的原则，页码和来源全程绑定，确保资料可用、可核、可追溯。

完整的构建流程分为 6 步：

1. **基础本体定义**：先定义 Document、Section、Component 等核心节点类型，冻结类型与关系的范围，避免无限制扩张图谱结构。
2. **PDF 章节定位**：按照物理页、章节、图表的层级，把每一块内容和 Evidence、SourceSpan 一一绑定，精准定位到原文坐标。
3. **内容结构化抽取**：从原文中提取组件、工序、步骤、参数、检查项和图表信息，转化为标准化的实体和关系。
4. **人工复核与规范化**：人工核对原文、页码、数值，确认关系方向和来源的准确性，修正自动抽取的误差。
5. **Validator 校验**：通过确定性校验器，检查字段、类型、端点、ID、单位、数值和证据的完整性，不合格的直接拦截。
6. **Neo4j 入库**：校验通过后，将 Graph Records 和 Evidence 以事务方式写入 Neo4j，固定版本，保证可查询、可回查。

这里有三层核心产物：

- **Graph Record**：实体、关系、属性，也就是结构化的知识事实；
- **Evidence**：页码、章节、原文依据，是事实的证据来源；
- **SourceSpan**：文档修订版本、物理页、坐标，是证据的精确定位。

三者一一对应，所有结论都能追溯到原文的具体位置。

### 2.2 智能体的问题解析工作流

这是我们系统的核心流程，基于 LangGraph 架构搭建，完整的推理链路分为 7 步，每一步都有明确的输入输出和校验逻辑。

1. **提出问题**：用户输入自然语言问题，比如 “高压主汽阀杆尺寸是否合理”“小修必检项目有哪些”。
2. **明确对象和关键词**：系统先锁定问题涉及的设备、部件，比如 “高压主汽阀杆”“支持轴承”，避免检索范围跑偏。
3. **确定查询范围**：判断用户是问参数、检查项，还是完整流程，对应不同的检索策略。
4. **查询相关内容**：按照对象和工序，在 Neo4j 图谱中定位对应的资料和证据。
5. **筛选有效依据**：从检索结果中筛选出匹配对象、动作、数值、单位、页码的有效证据，排除无关内容。
6. **复核结果**：核对答案的适用条件、数值范围和出处是否准确，有没有歧义或冲突。
7. **输出带出处的结果**：最终输出 “结论 + 物理页号 + 章节” 的规范回答；如果依据不足或者资料未明确，就标注情况，转人工复核，绝不输出没有证据支撑的结论。

这套工作流的核心特点是**结果可核对**—— 所有回答都保留了检查内容、参数条件和原始出处，不是黑盒式的 AI 回答，工程人员可以直接对照说明书验证。

对应到代码实现上，这条工作流的每个节点都有明确的模块分工：

- 问题规范化对应`normalize_question.py`，负责拆分问题目标、识别工程对象；
- 图谱检索对应`neo4j_retrieval.py`，负责按范围查询 Neo4j 并装配结果；
- 证据评估对应`assess_evidence.py`，处理歧义、数值校验、来源边界判断；
- 回答生成对应`generate_recommendation.py`，基于证据调用 LLM 组织语言；
- 最终校验对应`validate_response.py`，对生成的每一条事实主张做事实级校验，不通过就拦截。

### 案例演示

接下来用两个实际案例，看一下系统的实际效果。

**第一个案例：参数校验类问题** 用户问：“高压主汽阀杆与操纵座接头处的尺寸为 13.24mm，合理吗？” 系统会按设备、检查项和参数要求，定位到说明书物理页 37、第 2-10 节。 然后给出明确结论：13.24mm 不合理。标准尺寸 k 应为 12.7±0.25mm，允许范围 12.45~12.95mm，现场测量值超出上限 0.29mm，建议调整复装至规定范围后重新验收，调整方法需参照制造厂图纸。 回答里同时标注了页码和章节，方便用户直接翻查原文。

**第二个案例：清单类问题** 用户问：“小修必检项目包含什么？” 系统定位到说明书物理页 70、第 3-4 节，输出两条核心内容：一是检查各支持轴承和推力轴承的巴氏合金表面，排查磨损、拉毛、裂纹等问题；二是小修后要重做调节系统静态试验、危急遮断器喷油试验和超速试验，确认保安系统可靠。 同样附带了原文出处，保证答案的权威性。

---

## 三、任务总结与展望

最后第三部分，任务总结与展望。

### 阶段成果

目前我们已经完成的工作： 第一，完成了全部 94 个物理页的人工复核，形成了 1217 条结构化知识记录，从资料整理、校验、入库到结果复核的全流程已经完全贯通。 第二，针对《汽轮机本体安装及维护说明书》范围内的问题，比如参数校验、工序流程、检修清单等，系统大部分都能给出准确、带出处的回答。

### 当前边界

同时我们也明确了目前的能力边界： 第一，部分工程内容，比如吊耳卸扣的承载与孔径关系、汽封齿倒伏的判据、导向键热试验的验收条件等，原说明书里没有包含相关内容，需要后续补充更多资料才能支持分析。 第二，对于一些表述模糊的自然语言问题，工作流可能会错误识别关键词，导致输出相关但不匹配的答案，问题解析的精准度还有提升空间。 第三，回答校验器的功能还在完善，目前审核规则偏严格，会误拦截一些合规的回答，需要进一步调优规则。

### 后续工作

针对这些问题，我们后续的工作方向主要有三点： 第一，继续完善校验器规则，同时细化知识图谱的粒度，提升抽取和回答的准确率； 第二，在现有基础上，基于本体构建标准化的类，形成更完善的资料抽取规则，提升抽取效率和一致性； 第三，引入更多领域资料，扩充智能体的知识覆盖范围，逐步从单一说明书扩展到更完整的汽轮机安调知识库。

---

## 交流与讨论

以上就是本次项目的全部汇报内容，目前系统已经完成了核心链路的跑通和基础验证。接下来欢迎各位老师提问交流，我也准备了三个待讨论的问题供大家参考：

1. 后续的领域知识，是由甲方完全提供，还是甲方提供一部分、我们补充查找一部分文档？
2. 后续用户输入是否会包含图片，是否需要给智能体加入识图功能？
3. 是否需要保存上下文对话内容，支持多轮追问的场景？

我的汇报结束，谢谢大家。