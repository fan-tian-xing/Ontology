# 核电汽轮机安装调试智能体项目粗略步骤

## 一、项目定位

面向核电汽轮机机组的本机及辅机安装、调试场景，建设一个领域智能体，为业务人员提供设备知识查询、异常分析、规程检索、历史案例参考和处置建议。

项目采用以下总体路线：

```text
Agent 治理与编排层
        ↓
轻量语义本体 + 知识图谱 + 文档RAG + SOP/规则 + Skills
        ↓
输出带来源、适用范围、风险提示和不确定性的辅助建议
        ↓
业务专家审核后执行
```


---

## 二、总体流程

总体流程按以下顺序推进。第 3 步中的四项工作可以并行开展：

1. **确定试点问题**  
   选择一个范围明确、可以验证效果的典型场景。

2. **收集并审核领域资料**  
   汇总规范、说明书、SOP、历史案例和专家经验，并确认来源、版本和适用范围。

3. **并行建设四类知识能力**

   - 建立术语表和轻量语义本体；
   - 建立知识图谱 Schema 和实例数据；
   - 建设文档知识库和 RAG；
   - 整理 SOP、规则和专家经验。

4. **开发只读 Skills**  
   将设备查询、参数查询、规程检索、案例检索和确定性计算封装成可调用能力。

5. **搭建 Agent 编排与治理层**  
   让 Agent 根据问题选择知识来源和 Skills，并执行权限、安全与适用范围检查。

6. **构建可追溯 Demo**  
   展示问题识别、知识检索、规则命中、Skill 调用、引用来源和最终建议。

7. **专家评测、修正和迭代**  
   根据专家验收结果修改术语、本体、图谱、文档、规则、SOP 和 Skills。

---

## 三、阶段一：确定项目边界和试点场景

### 1. 明确用户和使用场景

- 主要用户：安装、调试、检修、技术支持和质量管理人员。
- 主要任务：知识问答、故障辅助排查、规程检索、历史案例检索和检查建议生成。
- 明确智能体只提供辅助意见，最终判断和操作由具备资质的业务人员完成。

### 2. 选择一个最小试点

先选择一个能够形成完整闭环的典型问题，例如：

```text
轴承磨损或振动异常辅助排查
```

需要覆盖：

- 涉及哪些设备和部件；
- 有哪些异常现象；
- 有哪些可能故障和原因；
- 应查询哪些参数；
- 应执行哪些检查步骤；
- 适用哪些规程和安全约束；
- 是否存在相似历史案例。

### 3. 确定不做什么

- 第一阶段不追求覆盖所有设备和故障；
- 不追求一次性建立完整的汽轮机领域本体；
- 不让大模型直接生成无来源的处置结论；
- 不让智能体自主执行设备控制和高风险数据写入。

---

## 四、阶段二：资料收集和知识治理

### 1. 收集资料

- 国家、行业和企业标准；
- 设计文件、技术规范和设备说明书；
- 安装调试规程和检查清单；
- 故障报告、质量问题报告和调试记录；
- 历史处置案例和经验反馈；
- 专家访谈、会议纪要和培训材料；
- 设备台账、测点说明和可用的监测数据。

### 2. 为资料增加元数据

每份资料至少记录：

- 文档名称和编号；
- 资料类型；
- 发布组织和责任人；
- 版本、发布日期和生效日期；
- 适用设备、机型、系统和工况；
- 权威等级；
- 保密等级和访问权限；
- 是否已经过专家审核；
- 被新版本替代或废止的情况。

### 3. 处理知识冲突

当公开资料、企业文件和专家经验发生冲突时，按照预先规定的权威等级处理，不由大模型自行决定。未经复核的专家经验应标记为“待验证”，不能直接作为正式操作依据。

---

## 五、阶段三：建立术语表和轻量语义本体

### 1. 统一术语

整理同义词、缩写、别名和上下位概念，例如：

```text
设备正式名称
设备简称
厂家型号
部件名称
异常现象名称
故障模式名称
```

避免同一设备在不同资料中因为名称不同而无法关联。

### 2. 建立核心类

初期可以包括：

```text
System                 系统
Equipment              设备
Component              部件
InstallationActivity   安装活动
CommissioningActivity  调试活动
Symptom                异常现象
FailureMode            故障模式
Cause                  原因
InspectionMethod       检查方法
Measurement            测量数据
Procedure              处置流程
ProcedureStep          流程步骤
SafetyConstraint       安全约束
Document               资料来源
ExpertExperience       专家经验
Skill                  智能体可调用能力
```

### 3. 建立核心关系

```text
Component partOf Equipment
Equipment partOf System
Equipment hasFailureMode FailureMode
FailureMode indicatedBy Symptom
FailureMode mayBeCausedBy Cause
Cause verifiedBy InspectionMethod
FailureMode handledBy Procedure
Procedure hasStep ProcedureStep
Procedure applicableTo Equipment
Procedure constrainedBy SafetyConstraint
Knowledge derivedFrom Document
Skill operatesOn Equipment
```

### 4. 建立规则和数据约束

- 使用本体公理表达稳定的类别和逻辑关系；
- 使用 SHACL 或其他校验机制检查字段、基数、数据类型和必填项；
- 使用独立规则表或规则引擎保存工程判断规则；
- 不把开放世界下的 OWL 限制误当作数据库必填校验。

---

## 六、阶段四：建立知识图谱

### 1. 设计图谱 Schema

明确：

- 节点类型；
- 关系类型；
- 节点和关系的属性；
- 每类对象的唯一业务标识；
- 数据来源和版本信息；
- 时间、工况和适用范围。

### 2. 导入实例数据

例如：

```text
某型号汽轮机
  └──包含→ 某型号轴承
              ├──出现→ 振动升高
              ├──可能故障→ 轴承磨损
              ├──可能原因→ 润滑油污染
              ├──验证方法→ 振动检测
              ├──适用流程→ 轴承异常排查SOP
              └──知识来源→ 调试报告2025-017
```

### 3. 建立图谱查询

优先支持以下关系路径：

```text
设备 → 部件 → 异常现象 → 故障模式
故障模式 → 可能原因 → 验证方法
故障模式 → 处置流程 → 流程步骤
设备/故障 → 历史案例 → 处置结果
知识结论 → 来源文档 → 版本和适用范围
```

---

## 七、阶段五：建设文档知识库和 RAG

### 1. 文档处理

- 解析 PDF、Word、表格和扫描件；
- 按章节、条款、设备和故障场景切分内容；
- 保留页码、章节、表格标题和图片说明；
- 将文档片段与本体中的设备、部件、故障和流程关联；
- 建立版本控制和失效文档过滤机制。

### 2. 检索策略

采用混合检索：

```text
关键词检索
+ 向量语义检索
+ 知识图谱关系检索
+ 元数据过滤
```

检索时必须考虑：

- 设备型号；
- 系统和部件；
- 安装或调试阶段；
- 当前工况；
- 文档版本；
- 用户权限；
- 资料权威等级。

### 3. 回答约束

- 结论必须尽量附带文档名称、版本、章节和页码；
- 没有找到可靠依据时明确回答“依据不足”；
- 多份资料冲突时展示冲突，不让模型自行选择；
- 不允许模型把推测写成已经确认的事实。

---

## 八、阶段六：整理 SOP、规则和专家经验

### 1. SOP 结构化

每个 SOP 至少包括：

- 名称和适用范围；
- 前置条件；
- 输入数据；
- 操作步骤；
- 判断分支；
- 异常处理；
- 安全条件和禁止条件；
- 输出结果；
- 完成标准；
- 版本和批准信息。

### 2. 专家经验结构化

将专家经验整理为：

```text
适用设备 + 工况 + 现象 + 判断依据 + 建议 + 风险 + 来源
```

专家经验需要经过复核、批准和版本管理，并与正式规程明确区分。

### 3. 规则结构化

规则可以采用：

```text
如果：出现哪些现象，并满足哪些参数条件
那么：优先检查哪些原因
但是：在哪些工况下规则不适用
依据：来自哪个正式文件或专家结论
```

---

## 九、阶段七：开发只读 Skills

第一阶段优先开发：

1. 设备与部件信息查询 Skill；
2. 监测参数和趋势查询 Skill；
3. 本体与知识图谱查询 Skill；
4. 规范和 SOP 检索 Skill；
5. 历史案例检索 Skill；
6. 阈值比较和确定性计算 Skill；
7. 报告或检查清单草稿生成 Skill。

每个 Skill 需要定义：

- 名称和用途；
- 输入与输出；
- 适用范围；
- 前置条件；
- 数据来源；
- 权限要求；
- 超时和失败处理；
- 审计日志；
- 是否需要人工确认。

写入生产系统、下发设备命令和修改安全参数的 Skill 不进入第一阶段。

---

## 十、阶段八：搭建 Agent 编排与治理层

Agent 收到问题后，按照以下流程工作：

```text
1. 识别用户意图、设备、部件、现象和工况
2. 判断信息是否充足，必要时向用户补问
3. 查询本体和知识图谱，确定概念与关系
4. 检索正式文档、SOP和历史案例
5. 根据需要调用监测查询或计算Skill
6. 应用安全规则、权限和适用范围过滤
7. 汇总证据，区分已知事实、推断和未知信息
8. 生成带来源、风险提示和后续检查项的回答
9. 将高风险结论提交业务专家审核
10. 保存完整的检索、调用和审核记录
```

系统架构保持模型无关，但实际部署只允许使用经过测试和批准的模型，不能把“不限制模型”理解为任何模型都能直接上线。

---

## 十一、阶段九：构建 Demo

Demo 建议至少展示一个完整案例：

```text
用户提出轴承磨损或振动异常问题
        ↓
Agent识别设备、部件和异常现象
        ↓
查询知识图谱中的故障关系
        ↓
检索适用规程和历史案例
        ↓
调用监测数据或参数计算Skill
        ↓
返回检查顺序、可能原因、依据和风险提示
```

Demo 不展示模型内部逐字思维过程，而展示可审计的决策轨迹：

- 如何理解用户问题；
- 识别了哪些设备和概念；
- 检索了哪些文档和案例；
- 命中了哪些规则和 SOP；
- 调用了哪些 Skills；
- 每个结论的证据来源；
- 哪些内容仍不确定；
- 为什么需要人工审核。

---

## 十二、阶段十：测试和验收

### 1. 建立专家测试集

测试问题应覆盖：

- 正常知识查询；
- 多设备、多部件的关系查询；
- 故障原因分析；
- SOP 和规范条款定位；
- 信息不足的问题；
- 资料冲突的问题；
- 超出知识范围的问题；
- 高风险或越权操作请求。

### 2. 评估指标

- 设备和术语识别准确率；
- 检索召回率和排序质量；
- 回答与引用资料的一致性；
- 无依据结论和幻觉率；
- SOP 步骤完整性；
- 规则和安全约束遵循率；
- Skill 调用正确率；
- 专家认可率；
- 回答可追溯率；
- 权限和保密控制有效性。

### 3. 专家验收

由领域专家逐条审核测试结果，记录错误类型并回流到：

```text
术语表、本体、知识图谱、文档切分、检索策略、规则、SOP或Skill
```

不要只通过修改 Prompt 临时掩盖底层知识问题。

---

## 十三、第一阶段建议交付物

- 一份试点范围和需求说明；
- 一份领域术语表；
- 一版本体类、属性和关系模型；
- 一版知识图谱 Schema 和试点实例数据；
- 一套带元数据和来源的文档知识库；
- 若干结构化 SOP、规则和专家经验；
- 一组只读 Skills；
- 一个能够展示决策轨迹的问答 Demo；
- 一套专家测试题、评测指标和验收记录；
- 一份权限、安全、审计和知识更新机制说明。

---

## 十四、后续迭代方向

1. 从单一故障扩展到更多设备、部件和安装调试场景；
2. 接入更多经过授权的企业内部资料和实时数据；
3. 建立知识变更、版本更新和专家复核流程；
4. 增加更多确定性计算和业务查询 Skills；
5. 建立多轮诊断、假设验证和缺失信息追问能力；
6. 在充分验证和审批的前提下，逐步接入工单、通知等低风险写入流程；
7. 长期再评估是否需要 Palantir 式的自动化操作闭环。

---

## 十五、需要始终坚持的原则

1. **最小够用**：从一个可验证的具体问题开始，不先追求大而全；
2. **来源优先**：任何重要结论都应能够追溯到正式资料或批准的专家经验；
3. **规则与生成分离**：确定性规则和计算由规则引擎或 Skill 完成，大模型主要负责理解、编排和表达；
4. **人工负责**：智能体提供辅助建议，不代替有资质人员作出安全相关决定；
5. **只读起步**：先做好查询和决策支持，再考虑业务写回；
6. **持续评测**：本体、知识、检索、规则和 Skills 都需要随着新资料和新案例持续更新。
