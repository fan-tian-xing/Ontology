# 汽轮机知识图谱与 LangGraph Demo：代码文件功能说明

> 本文档位于项目主文件夹，用于说明 `demo/` 目录下与业务运行逻辑直接相关的生产代码、图谱构建代码和运行契约。
>
> 已省略：包目录中的 `__init__.py`、`demo/langgraph_app/scripts/` 下的评估/审计脚本、`tests/` 下的测试代码，以及仅用于测试和验收的评测数据。这些文件不属于核心业务运行链路。

## 1. 项目整体职责

这个 Demo 将汽轮机安装、调试、检查和维护资料转成可以追溯的知识图谱，并通过 LangGraph 完成问答。核心原则是：

1. PDF 是正式知识来源；
2. PDF 内容先形成 Evidence，再由 Graph Record 表示结构化事实；
3. Graph Record 在写入 Neo4j 前由确定性的 Validator 校验；
4. LangGraph 运行时只从 Neo4j 获取证据，不直接读取 PDF，也不读取离线评测答案；
5. LLM 只负责根据检索证据组织回答，不能自行写入图谱；
6. LLM 生成的 Claim 还要经过事实级校验，校验失败时采用 fail-closed，不输出未经证实的工程建议。

## 2. 运行链路

```text
用户问题
   │
   ▼
normalize_question.py
   ├─ question_language.py：识别问句意图和工程对象
   ├─ question_targets.py：拆分问题目标和答案槽
   └─ keyword_catalog.py：从活动 Release 的实体目录匹配候选
   │
   ▼
neo4j_retrieval.py
   └─ Neo4jRetrievalFacade / Neo4jEvidenceTool
      ├─ 固定 CorpusRelease 和 DocumentRevision
      ├─ 执行实体、关系、Evidence 检索
      ├─ retrieval_policy.py：按问题类型选择边界
      ├─ object_scope.py：限制对象和上下文范围
      ├─ slot_coverage.py：判断答案槽覆盖情况
      └─ result_assembly.py：排序、去重、限制返回规模
   │
   ▼
assess_evidence.py
   ├─ ambiguity.py：候选冲突时拒绝猜测
   ├─ numeric.py：检查数值、单位和测量对象
   └─ source_scope.py：处理空白表格、页码-only 等来源边界问题
   │
   ▼
generate_recommendation.py
   ├─ 将 Neo4j Evidence 注入 LLM Prompt
   ├─ 支持主模型和备用模型故障转移
   └─ 将模型 Claim 绑定回已检索的 Fact/Evidence
   │
   ▼
validate_response.py
   ├─ 校验 Claim 的对象、动作、否定、数值、单位和证据归属
   └─ 失败时清理不可信回答或转为证据不足
   │
   ▼
CLI / FastAPI 返回答案、证据位置、校验状态和检索轨迹
```

图谱构建链路与问答链路分离：

```text
PDF / 人工复核页块
   │
   ├─ ocr_pages.py：生成辅助 OCR 页块
   ├─ extract_graph_records.py：从已复核页块提取 Graph Records
   ├─ build_corpus.py：生成页清单、Evidence、Graph Records 和 Release 元数据
   ├─ validator.py：校验节点、关系、属性、Evidence 和结构预算
   └─ load_graph.py：以参数化 Cypher 写入 Neo4j，并最后激活新 Release
```

## 3. 目录职责速览

| 目录 | 主要职责 |
|---|---|
| `demo/langgraph_app/src/` | 生产运行时的 LangGraph、API、CLI、状态模型和问答流程 |
| `demo/langgraph_app/src/retrieval/` | 问题解析、目标拆分、对象范围、槽覆盖和检索结果装配 |
| `demo/langgraph_app/src/grounding/` | 证据充分性、歧义、数值约束和来源边界策略 |
| `demo/langgraph_app/src/tools/` | Neo4j 工具、实体目录、Evidence 搜索和检索策略 |
| `demo/neo4j/loader/` | PDF 证据、Graph Record 构建、审计、OCR 和 Neo4j 加载 |
| `demo/neo4j/ontology/` | 运行时本体、Schema 和本体候选报告 |
| `demo/neo4j/ontology/protege/` | 与运行时契约对齐的隔离 Protégé OWL 影子 |
| `demo/neo4j/validator/` | Graph Record 和 Evidence 的确定性校验 |

---

## 4. LangGraph 应用包

### 4.1 `src` 核心运行模块

#### `demo/langgraph_app/src/state.py`

- **功能**：定义 LangGraph 节点之间共享的状态类型。
- **主要类型**：
  - `Observation`：输入问题中识别出的对象、现象、数值、单位和上下文；
  - `EvidenceItem`：从 Neo4j 返回的 Fact/Evidence、实体、关系、页码和来源文本；
  - `Claim`：LLM 产生的事实主张及其 Subject、Predicate、Object、数值、Fact ID、Evidence ID；
  - `State`：完整工作流状态，包括问题分类、查询词、问题目标、答案槽、检索证据、LLM 建议、校验状态和公开轨迹。
- **设计要点**：使用 `TypedDict`，使每个 LangGraph 节点都以显式字段读写状态，减少隐式全局变量。

#### `demo/langgraph_app/src/config.py`

- **功能**：从环境变量加载 LLM 和 Neo4j 配置，并提供运行前检查。
- **核心类型**：
  - `LLMEndpoint`：统一描述一个 LLM 提供方的名称、Base URL、模型、API Key 和超时；
  - `Settings`：保存主模型、两个可选备用模型和 Neo4j 连接参数。
- **关键函数**：
  - `_positive_float_env()`：读取并校验正数超时配置；
  - `Settings.from_env()`：加载 `.env` 后构造不可变配置；
  - `validate_for_neo4j()`：要求 Neo4j 密码存在；
  - `validate_for_llm()`：要求主 LLM 的 URL、模型和 API Key 存在；
  - `llm_endpoints()`：按主模型、备用模型 1、备用模型 2 顺序生成可用端点。
- **相关环境变量**：`LLM_BASE_URL`、`LLM_MODEL`、`OPENAI_API_KEY`、`LLM_FALLBACK_*`、`NEO4J_URI`、`NEO4J_USERNAME`、`NEO4J_PASSWORD`、`NEO4J_DATABASE`。

#### `demo/langgraph_app/src/errors.py`

- **功能**：定义 LLM 提供方全部失败时的专用异常 `LLMProvidersExhaustedError`。
- **使用位置**：`generate_recommendation.py` 抛出；`api.py` 将其转换为 HTTP 503；`cli.py` 将其转换为命令行错误。

#### `demo/langgraph_app/src/graph.py`

- **功能**：组装生产 LangGraph，并提供统一调用入口。
- **`build_graph()`**：按顺序连接 `normalize_question → neo4j_retrieval → assess_evidence → generate_recommendation → validate_response` 节点。
- **`invoke()`**：创建初始状态，调用已编译图，返回最终状态；也负责传递 `request_id`，便于 API/CLI/测试区分请求。
- **重要边界**：图中只有读取 Neo4j 的检索节点，没有写库节点；所有写库动作位于 `demo/neo4j/loader/`。

#### `demo/langgraph_app/src/api.py`

- **功能**：提供 FastAPI HTTP 接口。
- **路由**：`POST /recommend` 接收 `RecommendRequest`，调用 `graph.invoke()`，并将最终状态整理成 `RecommendResponse`。
- **异常映射**：LLM 全部失败返回 503；其他未处理异常返回 500。
- **返回内容**：答案、证据是否充分、校验状态、建议、缺失信息、检索轨迹、匹配实体、引用和 Claims。

#### `demo/langgraph_app/src/cli.py`

- **功能**：命令行查询入口。
- **`_display_answer()`**：移除回答中已经存在的内联页码引用，避免 CLI 同时重复打印；API 中仍保留结构化 citations。
- **`_public_trace()`**：从完整内部轨迹中抽取适合默认展示的字段，如活动 Release、Revision、查询词、根实体、Evidence 数和歧义状态。
- **`main()`**：解析问题和 `--debug-trace`，打印检索轨迹、输入警告、回答校验、LLM 建议和前五个证据位置。

### 4.2 grounding：证据充分性策略

#### `demo/langgraph_app/src/grounding/policies/base.py`

- **功能**：提供所有证据策略共享的输入输出协议。
- **`PolicyContext`**：包含问题、候选 Evidence、语义 Evidence、匹配实体、直接关系、测量术语、请求测量项、选中根实体文本和检索轨迹。
- **`PolicyResult`**：统一表示策略是否应用、是否通过、原因码、是否可重试、缺失信息和调试轨迹。
- **`EvidencePolicy`**：只读、无副作用的策略协议，要求实现 `evaluate()`。

#### `demo/langgraph_app/src/grounding/policies/ambiguity.py`

- **功能**：处理多个互不兼容候选同时命中、但问题没有明确对象的场景。
- **`remaining_engineering_scope()`**：删除“如何、检查、要求、多少”等通用问句词，只保留可能用于区分工程对象的文字。
- **`AmbiguityPolicy.evaluate()`**：在候选关系、测量词和对象范围之间做一致性判断；如果只剩多个不同的原子候选，则返回歧义失败并要求用户补充部件、工序或测量位置。
- **安全效果**：防止系统因为词面相似而把不同轴承、不同间隙或不同工序的事实混在一起。

#### `demo/langgraph_app/src/grounding/policies/numeric.py`

- **功能**：对数值型工程问题执行证据门禁。
- **主要逻辑**：识别参数是否存在直接数值约束、单位是否匹配、问题请求的测量项是否和选中根对象一致，并检查数值上下文中的独特词组。
- **关键函数**：`has_direct_numeric_constraint()` 识别直接约束；`numeric_constraint_matches_request()` 判断约束是否覆盖问题；`NumericConstraintPolicy.evaluate()` 将结果包装为统一 `PolicyResult`。
- **适用内容**：间隙、尺寸、温度、扭矩、载荷、位置和公差等参数。

#### `demo/langgraph_app/src/grounding/source_scope.py`

- **功能**：处理“来源本身不能提供答案”的问题语义。
- **`is_explicit_empty_source_conclusion_request()`**：识别用户明确限定只能从空白表格、空白记录或不可用片段推断工程结论的情况，并拒绝从空壳来源臆造数值或结论。
- **`is_bare_page_reference_question()`**：识别只问“某物理页写了什么”而未给出工程对象、参数或动作的问题，避免把页码当成语义查询条件。
- **内部识别函数**：处理来源范围扩展、记录字段请求、空记录声明和否定词，避免把普通设备负面描述误判为空来源问题。

### 4.3 models：API 数据模型

#### `demo/langgraph_app/src/models/response.py`

- **功能**：定义 API 请求和响应的 Pydantic 模型。
- **`RecommendRequest`**：要求非空 `question`，并提供默认 `request_id="api"`。
- **`RecommendResponse`**：描述答案、证据充分性、校验状态、建议、警告、缺失信息、轨迹、实体、引用和 Claims；列表和字典字段使用默认工厂，避免共享可变默认值。

### 4.4 nodes：LangGraph 节点

#### `demo/langgraph_app/src/nodes/normalize_question.py`

- **功能**：将自然语言问题转换成可检索的结构化问题状态。
- **主要阶段**：
  1. `_classify()`：识别问题类型，如流程、检查、参数、对比、清单、来源范围等；
  2. `_observations()`：提取现象、测量值、单位和上下文；
  3. `_fallback_terms()`：在复杂解析不足时生成保守查询词；
  4. `_routing_question()`：生成用于实体匹配和检索路由的规范化问题；
  5. `normalize_question()`：调用 `question_language.py`、`question_targets.py` 和 `keyword_catalog.py`，填充 `State` 的分类、查询词、焦点词、目标、答案槽和匹配实体。
- **设计目标**：先把一个复合问题拆成原子目标，再让后续检索按目标选择最小充分证据边界。

#### `demo/langgraph_app/src/nodes/neo4j_retrieval.py`

- **功能**：LangGraph 的 Neo4j 检索节点适配器。
- **`neo4j_retrieval()`**：读取规范化问题状态，调用默认 `Neo4jRetrievalFacade`，将 Evidence 和检索轨迹写回状态。
- **设计特点**：节点本身很薄，问题分类和复杂选择规则放在 `retrieval/` 与 `tools/`，便于单独测试。

#### `demo/langgraph_app/src/nodes/assess_evidence.py`

- **功能**：判断 Neo4j 返回的候选 Evidence 是否足以支持用户问题。
- **主要工作**：
  - 读取数值、单位、页码和文档标注；
  - 按 Evidence ID 去重；
  - 计算必需支持组和必需结构成员；
  - 检查直接参数约束、测量限定词、对象范围和证据完整性；
  - 应用歧义策略、数值策略和来源范围规则；
  - 输出 `evidence_sufficient`、`missing_information`、输入警告和更新后的检索轨迹。
- **核心入口**：`assess_evidence()`；其余函数是数值归一化、候选筛选、支持组覆盖和策略门禁的内部实现。
- **重要原则**：证据不是“越多越好”。如果新增的证据跨越了不兼容对象或测量位置，可能导致失败而不是提高置信度。

#### `demo/langgraph_app/src/nodes/generate_recommendation.py`

- **功能**：把已评估的 Neo4j Evidence 交给 LLM，生成带事实 Claim 的工程回答。
- **主要阶段**：
  - `_supplement_requests()`：根据证据不足情况生成需要用户补充的信息；
  - `_insufficient_evidence_response()`：在证据不足时生成保守结果；
  - `_invoke_llm_with_failover()`：按配置顺序调用主模型和备用模型；
  - `_eligible_fact_rows()`：只允许可回答的 Fact/Evidence 进入 Prompt；
  - `_bind_claims_to_retrieved_facts()`：将模型 Claim 绑定到已检索事实，禁止模型引入未检索的 Fact ID；
  - `_prompt()`：构造包含问题、检索意图、Evidence 和输出格式的提示词；
  - `generate_recommendation()`：完成上述流程并将回答、建议、Claim、引用和模型失败信息写回状态。
- **安全边界**：LLM 不生成 Cypher，不写入 Neo4j；它只能基于 Prompt 中已经提供的证据组织文本。

#### `demo/langgraph_app/src/nodes/validate_response.py`

- **功能**：对 LLM 返回的回答和 Claim 做事实级最终校验。
- **校验内容**：
  - Claim 是否绑定到检索到的 Fact/Evidence；
  - Subject 是否属于用户问题和选中对象；
  - Predicate/Action 是否被证据支持；
  - 数值、单位、比较符号和公差是否一致；
  - 否定动作是否被原文明确支持；
  - 页码和章节是否只是引用定位而非凭空增加事实；
  - 每个答案槽是否得到足够 Claim 覆盖。
- **关键函数**：`semantic_claim_qualifiers()` 提取限定词；`_quantities()`、`_answer_quantities()` 处理数值；`_actions()`、`_positive_actions()`、`_negated_actions()` 检查动作；`_validate_claims()` 执行 Claim 级检查；`validate_response()` 生成最终 `validation_status` 和 `validation_reasons`。
- **失败策略**：校验失败时不保留未经支持的工程建议正文，避免把模型的合理猜测误当成说明书结论。

### 4.5 retrieval：检索边界和问题解析

#### `demo/langgraph_app/src/retrieval/context.py`

- **功能**：定义一次检索请求的服务端上下文。
- **`RetrievalContext`**：固定 `corpus_release_id`、`document_revision_id`、允许的文档、审核状态、验证状态和是否只返回可回答 Evidence。
- **`query_parameters()`**：把不可变上下文转换成 Neo4j 查询参数。
- **重要作用**：所有查询在开始时固定活动 Release/Revision，避免同一个请求过程中混用不同版本的图谱。

#### `demo/langgraph_app/src/retrieval/facade.py`

- **功能**：为 LangGraph 提供一个薄的 Neo4j 检索门面。
- **`RetrievalPort`**：定义 `retrieve()` 协议，便于测试替换实现。
- **`Neo4jRetrievalFacade.pin_context()`**：获取活动语料上下文。
- **`Neo4jRetrievalFacade.retrieve()`**：固定或复用上下文，调用 `Neo4jEvidenceTool.query()`，并把 Release/Revision 写入轨迹。
- **设计取舍**：门面不放问题专用规则，只负责请求边界和工具委托。

#### `demo/langgraph_app/src/retrieval/fulltext.py`

- **功能**：构造安全、有限长度的 Neo4j Lucene 全文查询。
- **`build_fulltext_query()`**：从中文、英文、图号、单位和数值中提取 token，去重、排序、限制数量，并转义 Lucene 特殊字符。
- **安全作用**：集中处理用户文本进入 Neo4j 全文索引的转义，避免把问题内容解释成 Lucene 查询语法。

#### `demo/langgraph_app/src/retrieval/question_language.py`

- **功能**：对问题语言做轻量规范化和工程对象提取。
- **`GenericRequest`**：保存通用问句分类结果。
- **`normalize_interrogative_wording()`**：移除“如何、什么、多少”等问句外壳。
- **`parse_generic_request()`**：识别请求是方法、要求、参数、检查、流程或概览。
- **`is_generic_intent_fragment()`**：判断片段是否只是通用意图词；`extract_engineering_object()` 提取工程对象；`discriminative_question_bigrams()` 生成区分候选的中文二元片段。

#### `demo/langgraph_app/src/retrieval/question_targets.py`

- **功能**：将自然语言问题拆成结构化目标和答案槽，是检索边界控制的核心模块之一。
- **主要模型**：
  - `NumericSignature`：数值、单位和比较符号；
  - `QualifierContext`：部件、位置、状态、时间和测量限定词；
  - `AnswerSlot`：问题中一个独立的答案需求；
  - `QuestionTarget`：包含对象、谓词、目标类型、限定词和槽位的原子目标。
- **主要流程**：提取数值和测量项，识别并列对象，区分流程/检查/参数/对比意图，合并相关草稿，保持原文顺序并生成稳定 ID。
- **入口函数**：`parse_question_targets()` 解析目标；`extract_answer_slots()` 把目标转换为后续检索和响应校验使用的答案槽。
- **特殊处理**：支持复合协调、时间范围、名义测量对象、步骤序列和多个独立问题，避免把一个问题的多个事实压成一个不可验证的槽。

#### `demo/langgraph_app/src/retrieval/object_scope.py`

- **功能**：建立问题对象与图谱实体之间的范围契约，决定哪些父节点、子节点和邻接事实可以进入本次检索。
- **主要逻辑**：
  - 从实体、关系和 Evidence 生成语义文本；
  - 识别压力、位置、测量对象和程序内容等限定词；
  - 证明候选实体与问题之间的锚点关系；
  - 剪除宽泛祖先和只在词面上被包含的候选；
  - 保留有直接证据支持的最小对象范围。
- **核心类型和函数**：`ObjectScopeContract` 保存范围约束；`infer_question_facet()` 推断问题关注面；`build_object_scope_contract()` 生成最终契约。
- **作用**：防止“轴承”“间隙”“检查”等宽泛词把同一章节下不相关对象的答案一起拉入上下文。

#### `demo/langgraph_app/src/retrieval/result_assembly.py`

- **功能**：将底层 Neo4j 行转换成稳定、有限、可测试的检索结果。
- **主要逻辑**：识别持久化 Assertion 行、标题容器行和优先 Assertion；构建必需支持组；折叠 Evidence 等价根；按问题和根实体排序；最后限制结果行数。
- **关键函数**：`build_required_support_groups()`、`collapse_evidence_equivalent_roots()`、`assemble_result_row()`、`retrieval_rank()`、`bound_result_rows()`。
- **`RetrievalRankingContext`**：保存问题、目标、匹配实体和槽覆盖等排序上下文。
- **设计目的**：让检索结果具备确定性排序，避免 Neo4j 返回顺序变化导致答案漂移。

#### `demo/langgraph_app/src/retrieval/slot_coverage.py`

- **功能**：计算一个实体或 Assertion 是否覆盖问题的答案槽，并选择最小的原子根集合。
- **主要逻辑**：抽取实体和行的语义文本，比较对象、动作、测量、位置、压力段和枚举面；对行做槽级评分；过滤只起导航作用的结构关系。
- **关键类型和函数**：
  - `SlotRootSelection`：保存选择的根、原因和覆盖槽；
  - `slot_coverage_score()`、`bound_row_slot_coverage_score()`：计算覆盖分；
  - `select_minimum_atomic_roots()`：选择最小充分根集合；
  - `filter_answer_required_rows()`：过滤与答案无关的行；
  - `build_slot_support_coverage()`：生成每个答案槽对应的支持组；
  - `is_navigation_relation()`：区分导航关系和事实承载关系。
- **作用**：支持“一个槽一个事实边界”，减少跨事实拼接。

#### `demo/langgraph_app/src/retrieval/question_language.py`、`question_targets.py`、`object_scope.py` 的关系

三者不是三个独立的问答系统：`question_language.py` 负责词法和通用问句，`question_targets.py` 负责原子目标/答案槽，`object_scope.py` 负责把目标落实为图谱中的最小对象范围。它们的输出最终由 `retrieval_policy.py` 和 `slot_coverage.py` 用于筛选 Neo4j 结果。

### 4.6 tools：运行时检索工具

#### `demo/langgraph_app/src/tools/evidence_search.py`

- **功能**：对调用方提供的 Neo4j Evidence 行执行确定性搜索和排序，不自行连接数据库。
- **关键函数**：
  - `is_current_answer_evidence()`：判断 Evidence 是否处于当前活动版本且允许回答；
  - `search_question_evidence()`：按问题和候选池检索；
  - `rank_question_evidence()`：按词法、对象、关系和来源信息排序；
  - `search_question_targets()`：面向目标和答案槽搜索；
  - `search_term_evidence()`：按术语搜索；
  - `_result()`、`_rank()`：统一结果格式和排序分数。
- **异常**：没有候选池时抛出 `EvidenceCandidatePoolRequiredError`，避免工具偷偷扩大搜索范围。

#### `demo/langgraph_app/src/tools/keyword_catalog.py`

- **功能**：从当前活动 Release 读取实体目录并为问题路由匹配候选实体。
- **主要阶段**：
  - 解析活动 Release/Revision 边界；
  - 从 Neo4j 读取实体名、别名、类型和关系角色；
  - 生成词法变体并执行全文命中；
  - 根据问题形状、独立命中数和语义分数排序；
  - 返回匹配实体和调试信息。
- **关键入口**：`load_keyword_catalog()` 加载/缓存目录；`match_question()` 将问题匹配到实体。
- **异常**：`KeywordCatalogUnavailableError` 表示实体目录不可用，不能把目录不可用静默当成“没有匹配实体”。

#### `demo/langgraph_app/src/tools/neo4j_tool.py`

- **功能**：Neo4j 运行时访问层，负责活动上下文、参数化查询和 Evidence 行组装。
- **`Neo4jEvidenceTool`**：
  - `_get_driver()` 延迟创建 Neo4j Driver；
  - `active_context()` 查询当前活动 CorpusRelease 和 DocumentRevision；
  - `_unique_procedure_under_section()` 等辅助函数处理结构范围；
  - `_row()` 将查询记录标准化为运行时 EvidenceItem；
  - `query()` 根据术语、匹配实体、问题和答案槽执行有界检索，并返回 Evidence 与 trace；
  - `close()` 释放 Driver。
- **`Neo4jToolError`**：封装连接、活动版本或查询失败。
- **安全边界**：查询使用参数化 Cypher；该模块没有写库操作，也不读取 PDF 文件。

#### `demo/langgraph_app/src/tools/retrieval_policy.py`

- **功能**：检索和证据评估共享的问题路由策略，是运行时最复杂的规则模块之一。
- **结构边界规则**：
  - `structural_child_ids()`、`has_structural_children()`：识别 Procedure/Step 层级；
  - `select_complete_local_workflow_step_ids()`：选择完整局部流程；
  - `select_direct_qualifier_checklist_steps()`：选择直接限定词对应的检查步骤；
  - `scope_compatible_parent_step_ids()`：过滤范围不兼容的父步骤。
- **问题类型判断**：`is_checklist_question()`、`is_procedure_flow_question()`、`is_comparison_question()`、`requested_prefix_item_count()`。
- **目标选择**：`select_object_topic_leaf()`、`select_explicit_comparison_leaves()`、`select_explicit_atomic_targets()`、`select_primary_structural_match()`、`select_more_specific_contextual_leaves()`。
- **测量和限定词规则**：`select_complete_measurement_qualifier_leaves()`、`measurement_qualifier_evidence_complete()`、`select_specific_measurement_terms()`、`measurement_terms_match_text()`。
- **冲突规则**：`has_qualifier_conflict()`、`measurement_request_matches_text()` 检查测量对象、位置、压力段和数值限定词是否冲突。
- **重要原则**：优先选择语义上完整的叶节点，只有用户明确要求概览、清单或流程时才展开结构化父子节点。

---

## 5. Neo4j 图谱构建与加载代码

### 5.1 `demo/neo4j/loader/ocr_pages.py`

- **功能**：将图像型 PDF 生成按物理页切分的辅助 OCR 块，供人工复核和后续语义抽取使用。
- **关键函数**：
  - `_box_bounds()`：计算 OCR 坐标框；
  - `_engineering_column_items()`：识别工程文档中可能的列式文本；
  - `_page_id()`：生成稳定页 ID；
  - `run()`：按页渲染/读取 PDF，生成带页码、坐标、文本和 `pending` 状态的 JSONL。
- **重要边界**：OCR 是辅助材料，不是正式事实。默认页块为 `pending`，未被人工标记为 accepted 时，语义抽取不能直接提升为可回答 Evidence。

### 5.2 `demo/neo4j/loader/extract_graph_records.py`

- **功能**：从已经包含文本和坐标、并经过复核的页块中抽取 Schema 约束的 Graph Records。
- **关键函数**：
  - `_read_jsonl()`：读取页块；
  - `_parse_json()`：严格解析 LLM 返回的 JSON；
  - `_prompt()`：构造 ontology/schema 约束提示词；
  - `_bind_evidence()`：把模型返回的 Evidence 重新绑定到输入页块的原文和坐标；
  - `extract()`：调用 LLM、绑定 Evidence、执行 Validator 并输出记录。
- **安全边界**：不生成或执行 Cypher；不允许模型直接决定最终 Neo4j 写入内容；输出必须经过 `validator.validate_file()`。

### 5.3 `demo/neo4j/loader/build_corpus.py`

- **功能**：构建可复现的 PDF 语料、Evidence、Graph Records、来源条款和 Release 元数据，是图谱构建主脚本。
- **基础工具**：`page_section_number()`、`section_entity()`、`page_dimensions()`、`sha256()`、`write_jsonl()` 处理页码、章节、尺寸、哈希和 JSONL 输出。
- **实体与记录构建**：`evidence()` 创建 Evidence；`entity()` 创建语义实体；`record()` 创建 Graph Record；`canonicalize_entity_definitions()`、`canonicalize_records()` 统一 ID、排序和字段。
- **OCR/来源处理**：`_normalize_ocr_text()`、`_ocr_page_text()`、`_load_page_text_overrides()` 读取人工复核文本；`build_page_manifest()` 构建物理页清单；`_split_reviewed_source_clauses()`、`build_source_clause_records()` 将复核条款变成来源层记录。
- **来源覆盖和语义绑定**：`_coverage_text()`、`_clause_critical_literals()`、`_claim_payload()`、`_literal_in_payload()` 检查原文关键字是否被 Graph Record 保留；`bind_unmodeled_clauses_to_semantic_owners()` 将尚未独立建模的条款绑定到合适的语义拥有者。
- **结构精化**：
  - `build_reviewed_outline_heading_evidence()`、`build_reviewed_replacement_evidence()`：生成审核后的标题和替换证据；
  - `refine_reviewed_outline_hierarchy()`：修正章节/步骤层级；
  - `normalize_structural_orders()`：规范兄弟节点顺序；
  - `refine_flow_clearance_hierarchy()`：修正流程和间隙结构；
  - `refine_bearing_box_table_hierarchy()`：修正轴承箱/表格层级；
  - `refine_coupling_hierarchy()`：修正联轴器层级；
  - `collapse_generated_navigation_steps()`：合并不应作为独立事实的导航步骤；
  - `refine_hp_valve_actuator_hierarchy()`：修正高压阀执行机构层级；
  - `refine_section_214_hierarchy()`：处理第 2-14 节特殊结构。
- **输出组装**：`build_supplemental_records()`、`build_curated_records()`、`build_layout_records()`、`build_additional_page_records()` 组成不同来源的记录；`add_document_section_hierarchy()` 添加文档章节关系；`apply_release_metadata()` 绑定 CorpusRelease、DocumentRevision 和哈希。
- **入口**：`main()` 负责按照固定顺序读取正式 PDF、生成数据文件、执行校验，并输出当前发布信息。
- **注意**：这是一个大型、强领域规则脚本。修改它时应同时检查 Validator、图结构审计和来源覆盖审计的结果。

### 5.4 `demo/neo4j/loader/load_graph.py`

- **功能**：将经过 Validator 校验的 Graph Records 和 Evidence 以参数化 Cypher 写入 Neo4j。
- **查询构造函数**：`_node_query()`、`_relationship_query()`、`_evidence_query()`、`_support_query()`、`_source_span_query()`、`_evidence_span_query()` 分别构造不同类型节点、关系和支持边的 MERGE/更新语句。
- **数据清理与映射**：
  - `_semantic_node_properties()`、`_semantic_evidence_properties()` 清理可写入属性；
  - `_runtime_evidence_records()` 过滤当前活动文档和状态；
  - `_materialize_evidence()` 写入独立 Evidence；
  - `_support_entity_ids()` 根据 `support_contract.py` 选择 Assertion 的语义拥有端点；
  - `_remove_legacy_evidence_property()` 清除旧版直接挂在实体上的 Evidence 属性。
- **`load()`**：读取数据、计算哈希、校验/创建 Neo4j 约束和全文索引，在单个受控事务中写入实体、Evidence、支持边和 Assertion 关系；最后才将新 Release 设为 active，以便事务失败时保留旧活动版本。
- **命令行**：支持 `--reset`，但只清理当前汽轮机图谱和本手册 Evidence，不应使用破坏性容器卷删除。

### 5.5 `demo/neo4j/loader/audit_graph.py`

- **功能**：对已持久化的整张图谱执行与问题无关的结构和来源覆盖审计。
- **主要检查**：循环、多个结构父节点、事实是否保持关键字、Evidence 是否覆盖来源条款、来源页是否匹配。
- **关键函数**：`_cycles()` 检查环；`_multiple_structural_parent_rows()` 检查多父节点；`_literal_preserved()` 和 `_payloads_cover_clause()` 检查原文关键事实是否被 Graph Record 保留；`main()` 输出 JSON 审计结果。
- **边界**：只读 JSONL 并输出诊断，不生成问题，不修改图谱。

### 5.6 `demo/neo4j/loader/audit_pdf_graph_alignment.py`

- **功能**：逐页审计 PDF 条款覆盖和图谱大纲粒度。
- **核心逻辑**：读取 accepted `EV_CLAUSE_*` 条款，寻找最佳对应 Graph Record，并标出多个兄弟 Step 可能由同一打印条款错误拆出的情况。
- **关键函数**：`_best_clause()` 选择最佳条款匹配；`audit()` 产生按页、按条款的覆盖报告；`main()` 提供命令行入口。
- **用途**：帮助人工判断 PDF 是否给出了独立项目编号，而不是把相邻文本自动当成独立步骤。

### 5.7 `demo/neo4j/validator/support_contract.py`

- **功能**：定义每种 Assertion 关系的 Evidence 支持拥有端点。
- **`SUPPORT_OWNER_ROLE_BY_RELATION`**：维护关系到 `source`/`target` 的映射，例如 `PROCEDURE_HAS_STEP` 由 target Step 拥有支持，`COMPONENT_PART_OF` 由 source Component 拥有支持。
- **`support_owner_role()`**：拒绝没有契约的关系；`support_owner_endpoint()`：返回实际拥有 Evidence 支持的端点。
- **重要性**：Validator、Loader、图审计和质量 Cypher 都必须共享该表，避免不同模块对同一关系解释不一致。

### 5.8 `demo/neo4j/validator/validator.py`

- **功能**：对 Graph Record 和 Evidence 执行确定性、无 LLM 的白名单校验。
- **检查项目**：
  - ID 格式和必填字段；
  - 节点类型、关系类型、端点类型和属性白名单；
  - Evidence 状态、来源、页码和绑定关系；
  - 参数约束，区分严格边界、精确值和名义值；
  - correction ID 和修正关系；
  - 结构预算、超级节点、多父节点和循环；
  - 每条 Assertion 是否有 Evidence 支持。
- **关键入口**：`validate_record()` 校验单条记录；`load_records()` 读取 JSONL；`validate_structural_budgets()` 进行问题无关的结构约束检查；`validate_file()` 校验 Graph Records；`validate_evidence_file()` 校验独立 Evidence 清单。
- **异常**：`ValidationError` 表示记录不允许进入加载阶段。

---

## 6. Ontology 与 Protégé 文件

### 6.1 `demo/neo4j/ontology/ontology_extraction_framework.py`

- **功能**：提供 ontology-first 的候选报告和冻结入口。
- **常量**：`ROOT_NODES` 定义根节点白名单；`ROOT_TO_REFINEMENTS` 定义 Component、Procedure、Parameter 等根节点允许的细化方向。
- **`build_candidate_report()`**：计算正式 PDF SHA-256，记录复核页、根节点和冻结规则。
- **`write_candidate_report()`**：将报告写到 JSON 文件。
- **原则**：发现未知类型、属性或端点时记录 ontology gap，不在普通抽取过程中自动扩展 Schema。

### 6.2 `demo/neo4j/ontology/protege/check_alignment.py`

- **功能**：检查隔离的 Protégé OWL 影子是否与运行时 `ontology.yaml`、`schema.json` 和 Python 契约完全对齐。
- **解析策略**：只实现受控的 JSON、有限 YAML 和受控 RDF/XML 子集。未知结构、重复 JSON 键、额外 OWL 元素和不支持的表达式都 fail-closed。
- **主要阶段**：
  - `_load_limited_yaml()`、`_load_schema()`：读取运行时契约；
  - `_parse_owl()`：读取 OWL 类、关系、标签、domain/range 和继承；
  - `_validate_xml_surface()`：限制 RDF/XML 表面语法；
  - `_validate_subclass_graph()`：检查继承图；
  - `_validate_domain_or_range()`：检查关系端点；
  - `validate_alignment()`：汇总全部对齐错误；
  - `main()`：命令行门禁。
- **`AlignmentSummary`**：记录检查统计；`AlignmentError`：表示对齐失败。
- **边界**：OWL 文件只是设计和评审影子，不会自动改变运行时本体、Loader、Neo4j 数据或活动 Release。

---

## 7. Neo4j 运行配置和 Cypher 文件

### 7.1 `demo/langgraph_app/pyproject.toml`

- **功能**：定义 LangGraph 应用的 Python 包元数据、依赖、测试可选依赖和 setuptools 包目录。
- **主要依赖**：FastAPI、LangChain OpenAI、LangGraph、Neo4j Driver、pypdf、Pydantic、python-dotenv、Uvicorn。
- **版本边界**：要求 Python `>=3.11`，依赖采用主版本范围约束，避免不受控升级破坏运行契约。

### 7.2 `demo/langgraph_app/.env.example`

- **功能**：LangGraph 运行所需环境变量模板。
- **配置分组**：主 LLM、两个可选备用 LLM、各提供方超时、Neo4j URI/用户名/密码/数据库。
- **使用注意**：真实 `.env` 只用于本地运行，不应提交真实密钥；主模型和备用模型必须使用与代码兼容的 OpenAI API 协议。

### 7.3 `demo/neo4j/.env.example`

- **功能**：Docker Compose 的 Neo4j 密码、网络名等配置模板。
- **注意**：`NEO4J_PASSWORD` 必须与 LangGraph 应用连接时使用的密码一致。

### 7.4 `demo/neo4j/docker-compose.yml`

- **功能**：启动 Neo4j 5 容器 `turbine-kg-neo4j`。
- **配置**：映射 Browser `7474` 和 Bolt `7687`；设置堆内存；配置健康检查；把数据和日志挂载到上级 `docker-data/`；通过 `turbine_kg` 网络连接。
- **持久化含义**：容器删除不等于数据删除，实际数据位于挂载目录。不要随意执行 `docker compose down -v`，否则可能删除 Neo4j 数据卷。

### 7.5 `demo/neo4j/cypher/constraints.cypher`

- **功能**：定义 Neo4j 的唯一性约束和基础索引，保证 Entity、Evidence、SourceSpan、DocumentRevision、CorpusRelease 的 ID 唯一。
- **使用场景**：初始化或人工检查数据库结构；生产 Loader 也会以幂等方式确保必要约束和全文索引存在。

### 7.6 `demo/neo4j/cypher/kg_architecture.cypher`

- **功能**：表达知识图谱架构和关键节点/关系的示例或初始化 Cypher。
- **内容重点**：文档、章节、语义实体、Assertion、Evidence、SourceSpan、DocumentRevision、CorpusRelease 的连接方式。
- **注意**：该文件是数据库结构说明/操作脚本，不替代 Python Validator 和 Loader 的运行时约束。

### 7.7 `demo/neo4j/cypher/quality_checks.cypher`

- **功能**：执行图谱质量检查。
- **检查方向**：Evidence 支持拥有者、结构多父节点、超级节点、活动 Release、来源范围和关系完整性。
- **一致性要求**：其中的支持拥有者关系列表必须和 `validator/support_contract.py` 同步，相关测试会阻止契约漂移。

### 7.8 `demo/neo4j/ontology/ontology.yaml`

- **功能**：运行时本体的声明式白名单，定义允许的节点类型、关系类型、节点属性、关系属性、关系端点和版本号。
- **使用者**：`validator.py` 用它拒绝未知结构；`protege/check_alignment.py` 用它和 OWL 影子对账；构图和加载代码据此决定哪些 Graph Record 形状合法。
- **性质**：这是运行时契约的一部分，不是普通说明文档。修改后必须同步 Schema、Validator、Loader、Cypher 质量检查和相关测试。

### 7.9 `demo/neo4j/ontology/schema.json`

- **功能**：以 JSON Schema 形式描述 Graph Record、Evidence 和相关字段的结构约束。
- **作用**：补充 `ontology.yaml` 的类型/关系白名单，约束字段类型、必填属性、数组结构和枚举值；也是 Protégé 对齐检查的输入之一。

### 7.10 `demo/neo4j/ontology/root_candidates.json`

- **功能**：记录从 PDF 复核中得到的本体根节点和候选细化方向。
- **作用**：为 ontology-first 构图提供审阅依据，帮助把未知概念放入 ontology gap，而不是直接在抽取过程中自由扩展运行时 Schema。

---

## 8. 文件之间的关键依赖关系

### 8.1 问答侧

```text
api.py / cli.py
       │
       ▼
graph.py
       │
       ├── normalize_question.py
       │      ├── question_language.py
       │      ├── question_targets.py
       │      └── keyword_catalog.py
       │
       ├── neo4j_retrieval.py
       │      ├── retrieval/facade.py
       │      └── tools/neo4j_tool.py
       │             ├── retrieval_policy.py
       │             ├── object_scope.py
       │             ├── slot_coverage.py
       │             └── result_assembly.py
       │
       ├── assess_evidence.py
       │      ├── grounding/policies/ambiguity.py
       │      ├── grounding/policies/numeric.py
       │      └── grounding/source_scope.py
       │
       ├── generate_recommendation.py
       │      └── config.py / errors.py
       │
       └── validate_response.py
              └── state.py / models/response.py
```

### 8.2 构图侧

```text
ontology.yaml + schema.json
       │
       ▼
build_corpus.py / extract_graph_records.py
       │
       ▼
validator.py + support_contract.py
       │
       ▼
load_graph.py
       │
       ▼
Neo4j
       │
       ├── audit_graph.py
       ├── audit_pdf_graph_alignment.py
       └── quality_checks.cypher
```

### 8.3 OWL 影子侧

```text
ontology.yaml + schema.json + support_contract.py
       │
       ▼
protege/check_alignment.py
       │
       ▼
protege/turbine-ontology.owl
```

OWL 影子是设计审阅产物，不是运行时写库输入。

## 9. 修改代码时的建议

| 修改内容 | 需要重点检查的文件 |
|---|---|
| 问题解析、答案槽、并列问题 | `normalize_question.py`、`question_language.py`、`question_targets.py` |
| 对象范围和候选裁剪 | `object_scope.py`、`slot_coverage.py`、`retrieval_policy.py` |
| Evidence 充分性 | `assess_evidence.py`、`grounding/policies/*` |
| LLM 输出和 Claim 校验 | `generate_recommendation.py`、`validate_response.py` |
| Neo4j 查询 | `neo4j_tool.py`、`retrieval/facade.py`、`result_assembly.py` |
| Graph Record Schema | `ontology.yaml`、`schema.json`、`validator.py` |
| Evidence 支持关系 | `support_contract.py`、`load_graph.py`、`quality_checks.cypher` |
| PDF 构图和来源绑定 | `build_corpus.py`、`extract_graph_records.py`、`audit_graph.py`、`audit_pdf_graph_alignment.py` |
| OWL/Protégé 设计影子 | `protege/check_alignment.py`、`turbine-ontology.owl` |
| API 或 CLI 输出 | `api.py`、`cli.py`、`models/response.py` |

## 10. 数据文件边界

以下文件虽然是项目的重要组成部分，但它们是数据或文档，不属于“代码文件”：

- `demo/neo4j/data/graph_records.jsonl`：Graph Records/Assertion 数据；
- `demo/neo4j/data/evidence.jsonl`：运行时和审计 Evidence；
- `demo/neo4j/data/corpus_manifest.json`：活动语料、文档修订和哈希清单；
- `demo/neo4j/data/entity_aliases.json`：实体别名数据；
- `demo/neo4j/data/ontology_gaps.jsonl`：Ontology gap 记录；
- `demo/neo4j/data/reviewed_page_transcripts.jsonl`：人工复核页级转录；
- `demo/evaluation/`：盲测问题、预期答案、固定回归结果和冻结清单；
- `demo/neo4j/ontology/ontology.yaml`、`schema.json`、`root_candidates.json`：虽然是配置/契约文件，但不是 Python 代码，属于运行时 Schema 数据；
- `demo/neo4j/ontology/protege/turbine-ontology.owl`：OWL 设计影子，不参与运行时写库。

## 11. 一句话总结

这个项目不是“LLM 直接读 PDF 并回答”的简单问答脚本，而是一个由 **来源证据、Schema 校验、Neo4j 图谱、问题目标拆分、证据策略、LLM 组织回答和 Claim 级校验** 共同组成的可追溯工程知识问答 Demo。

