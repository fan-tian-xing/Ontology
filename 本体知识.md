# RDF 资源描述框架
- 核心思想：把所有知识拆成一条条**三元组**。
- RDF 只规定 “主谓宾” 结构，**本身没有类、继承、推理规则**。
- 文件格式：`.ttl`(Turtle，人看得懂)、`.rdf/xml`。


**本体：是对一个领域内客观事物做的显式、形式化的概念规范框架。它用类、属性、公理约束，把现实世界的概念、特征、概念间关系明确定义出来，供机器读懂并推理；本体主要由 TBox 术语层 + 可选的 ABox 实例层两部分组成。**

1. **TBox（术语框，本体的骨架）**
    
    定义领域有哪些**类（Class）**、属性、公理约束：

- 类：代表一类事物的抽象概念，如「大学」「水」「氢原子」；
- 属性：分为数据属性、对象属性，用来描述特征或者连接事物；
- 公理：子类、传递属性、不交类、等价类这类规则，赋予机器推理能力。

> TBox 只定义模式，不写具体现实个体。

2. **ABox（断言框，实例部分）**
    
    填充 TBox 框架的具体事实，存放**个体**以及个体之间的关系断言。

> 一份 OWL 本体文件语法上可以同时包含 TBox+ABox；工程上一般把本体骨架 (TBox) 和大批量实例 (ABox) 分开存储。

> 关键点：本体不是一堆字符串名字。它用**URI**唯一标识每一个类、每一个个体，以此解决自然语言字符串的歧义。各种别名、外号只是字符串字面量，通过别名属性挂载到 URI 实体上，本身不是本体实体。